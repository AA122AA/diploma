version: "3.2"
services:
  elasticsearch:
    image: 
    containter_name: elasticsearch
    ports:
      - "5000:5000"
    volumes:
      - .:/code
      - logvolume01:/var/log
    links:
      - redis
  redis:
    image: redis
volumes:
  logvolume01: {}

